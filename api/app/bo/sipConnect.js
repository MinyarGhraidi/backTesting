import {
    Invitation,
    Inviter,
    InviterOptions,
    Referral,
    Registerer,
    RegistererOptions,
    Session,
    SessionState,
    UserAgent,
    UserAgentOptions,
    InvitationAcceptOptions} from "sip.js";
const transportOptions = {
    server: "wss://example.com:8443"
};
const SipConnect()
const userAgent = new UserAgent({transportOptions});
userAgent.start().then(data => {

});